name: Task Authority Live DeepSeek Evaluation (Archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Live evaluation archived
        run: |
          echo "This workflow preserves the historical live-evaluation entry point."
          echo "No model API calls are executed."
          echo "Aggregate evidence is documented in benchmarks/task-contracts/EVALUATION.md."
          echo "Artifact identities are recorded in benchmarks/task-contracts/ARTIFACT_MANIFEST.md."
